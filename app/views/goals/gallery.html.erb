<h2><%= @goal.name %></h2>
<p>
  <%= @goal.photos.count == 0 ? "0 pin" : pluralize(@goal.photos.count, "pin") %>
</p>

<%= link_to "Retour à l'objectif", goal_path(@goal) %>
<div class="gallery-grid">
  <% @goal.photos.each do |photo| %>
    <div class="gallery-item">
      <%= image_tag photo, width: 300 %>

      <%= button_to "Supprimer",
        "/goals/#{@goal.id}/photos/#{photo.blob.id}",
        method: :delete,
        data: { turbo_confirm: "Supprimer cette photo ?" } %>
    </div>
  <% end %>
</div>
